# 변경 이력

## 2026-07-24

### 추가

- 설정 화면에 `시작프로그램 등록` 체크박스를 추가했습니다.
- 사용자가 설정을 저장할 때 Windows 시작프로그램 등록 여부를 선택할 수 있도록 했습니다.
- 현재 사용자 레지스트리의 `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run` 경로를 사용하므로 관리자 권한 없이 등록 및 해제할 수 있습니다.
- Windows 로그인 시 프로그램이 `--tray` 옵션으로 실행되어 설정 창을 표시하지 않고 시스템 트레이에서 시작됩니다.
- 설정 화면을 열 때 실제 시작프로그램 등록 상태를 확인하여 체크박스에 반영합니다.
- 시작프로그램 등록 또는 해제에 실패하면 설정 화면에 오류 메시지를 표시합니다.
- 기존 Core 라이브러리를 재사용하는 Linux 명령줄 클라이언트를 추가했습니다.
- Linux에서 계정, 대상 호스트, 지정 IP, 업데이트 주기를 대화형으로 설정할 수 있습니다.
- Linux DDNS 즉시 업데이트 및 주기적 백그라운드 실행을 지원합니다.
- 관리자 권한 없이 등록할 수 있는 systemd 사용자 서비스 설치·해제·상태 확인 명령을 추가했습니다.
- Linux 설정 파일과 systemd 서비스 파일을 사용자 전용 권한으로 보호합니다.

### 배포

- 다음 Windows 아키텍처용 Release 단일 실행 파일을 게시했습니다.
  - `win-x64`
  - `win-x86`
  - `win-arm64`
- 배포 결과는 `publish-artifacts` 디렉터리의 아키텍처별 하위 디렉터리에 저장했습니다.
- 배포 파일은 framework-dependent 방식이므로 실행 환경에 .NET 10 Desktop Runtime이 필요합니다.
- Linux용 `linux-x64`, `linux-arm64` 단일 실행 파일 게시 구성을 추가했습니다.

### 검증

- 솔루션 빌드 결과: 경고 0개, 오류 0개
- 세 아키텍처의 게시 작업이 모두 정상적으로 완료되었습니다.
- `win-x64` 실행 파일에서 `DNSEver DDNS 설정` 창이 생성되는 것을 확인했습니다.

### 수정

- DNSEver가 호스트별로 `721 Already Updated`를 반환한 경우 이미 동일한 IP가 반영된 정상 상태로 처리하도록 성공 판정 로직을 수정했습니다.
- 모든 호스트 결과가 `720 Update Success` 또는 `721 Already Updated`이면 DDNS 반영 성공으로 표시합니다.
- 긴 상태 메시지가 하단 레이아웃의 너비를 확장하여 입력 컨트롤과 버튼이 오른쪽으로 밀리는 문제를 수정했습니다.
- 상태 메시지 영역의 너비를 폼 내부로 제한하고 공간이 부족한 경우 말줄임표로 표시하도록 변경했습니다.
- 제품 및 파일 버전을 `1.0.2026.724`, 게시 파일명 표시 버전을 `1.0.2026.0724`로 변경했습니다.
- 프로젝트별 중복 버전 설정을 제거하고 `Directory.Build.props`에서 버전을 통합 관리하도록 변경했습니다.
