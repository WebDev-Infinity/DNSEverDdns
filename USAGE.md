# DNSEver DDNS Windows Client 사용 방법

이 문서는 DNSEver DDNS Windows Client를 처음 설정하고 사용하는 방법을 설명합니다.

## 1. DNSEver 사이트에서 사전 설정

프로그램을 사용하기 전에 DNSEver 사이트에서 다이나믹 DNS 사용을 허용하고 인증 코드를 생성해야 합니다.

1. DNSEver 사이트에 로그인합니다.
2. `내정보` 메뉴로 이동합니다.
3. `DNS 환경설정`으로 이동합니다.
4. `다이나믹 DNS 허용여부`를 설정합니다.
   - `선택한 레코드만 허용`
   - 또는 `모두 허용`
5. 다이나믹 DNS 인증 코드를 생성합니다.

생성한 인증 코드는 DNSEver 로그인 비밀번호가 아니라, 다이나믹 DNS 업데이트에 사용하는 별도 인증 코드입니다.

## 2. 프로그램에서 계정 정보 입력

프로그램을 실행한 뒤 설정 화면에서 다음 값을 입력합니다.

1. `아이디`에 DNSEver 사용자 아이디를 입력합니다.
2. `인증 코드`에 DNSEver 사이트에서 생성한 다이나믹 DNS 인증 코드를 입력합니다.
3. `호스트 조회` 버튼을 클릭합니다.

인증 정보가 올바르면 DNSEver에 등록된 호스트 목록이 표시됩니다.

## 3. 업데이트할 호스트 선택

호스트 목록에서 DDNS 업데이트를 적용할 호스트를 체크합니다.

- 선택한 호스트만 업데이트 대상이 됩니다.
- DNSEver 사이트에서 `선택한 레코드만 허용`을 사용한 경우, DNSEver 쪽에서도 해당 레코드가 허용되어 있어야 합니다.

## 4. IP 적용 방식 선택

기본적으로 DNSEver 서버가 감지한 현재 접속 IP를 사용해 DDNS를 업데이트합니다.

특정 IP를 직접 적용하려면 다음과 같이 설정합니다.

1. `IP 조회` 버튼으로 현재 공인 IP를 확인합니다.
2. 조회한 IP를 그대로 사용하려면 `지정 IP 적용` 버튼을 클릭합니다.
3. 직접 입력할 IP가 있다면 `지정 IP`에 원하는 IP 주소를 입력합니다.

`지정 IP`를 비워두면 DNSEver 서버가 감지한 IP를 사용합니다.

## 5. 업데이트 주기 설정

`주기(분)`에서 자동 업데이트 간격을 설정합니다.

- 기본값은 60분입니다.
- 너무 짧은 주기로 자주 업데이트하면 DNSEver에서 `320: Too Many updates sent` 응답을 받을 수 있습니다.
- 일반적인 환경에서는 60분 이상을 권장합니다.

## 6. 저장 및 실행

설정이 끝나면 `저장` 버튼을 클릭합니다.

저장 후 프로그램은 선택한 호스트에 대해 DDNS 업데이트를 수행하고, 이후 설정한 주기마다 자동으로 업데이트합니다.

프로그램은 Windows 트레이 아이콘으로 상주합니다.

- 트레이 아이콘 더블 클릭: 설정 창 열기
- 트레이 아이콘 우클릭 > `설정 열기`: 설정 창 열기
- 트레이 아이콘 우클릭 > `지금 업데이트`: 즉시 DDNS 업데이트
- 트레이 아이콘 우클릭 > `종료`: 프로그램 종료

## 7. 설정 저장 위치

설정 파일은 현재 Windows 사용자 로컬 앱 데이터 폴더에 저장됩니다.

```text
%LOCALAPPDATA%\DNSEverDdns\settings.json
```

DDNS 인증 코드는 Windows DPAPI를 사용해 현재 Windows 사용자 계정 범위로 암호화되어 저장됩니다. 다른 PC나 다른 Windows 사용자 계정에서는 일반적으로 복호화되지 않습니다.

로그 파일은 다음 위치에 저장됩니다.

```text
%LOCALAPPDATA%\DNSEverDdns\logs
```
